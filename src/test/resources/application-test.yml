spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

tmap:
  app-key: "TBYjLMpNBS90Edm8iyvh43V8bYVBpGGA65llF4Pl" # 실제 키를 입력하세요
  base-url: "https://apis.openapi.sk.com"
