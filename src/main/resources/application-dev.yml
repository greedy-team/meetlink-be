spring:
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org.hibernate.SQL: info
    com.greedy.meetlink: debug
cors:
  allowed-origins: >
    http://localhost:5173,
    https://dev.meetlink.now
